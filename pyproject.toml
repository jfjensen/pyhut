# Build configuration. Metadata lives in setup.cfg so that the package also
# installs on the older pip/setuptools shipped with JetPack 4.x (Python 3.6).
# The build-system pin below is deliberately conservative for that reason.
[build-system]
requires = ["setuptools>=40.8.0", "wheel"]
build-backend = "setuptools.build_meta"
